<ui:FluentWindow x:Class="Kys_cad_plugin.Views.Txt17InputUI"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 Title="17사 TXT 좌표 입력기" 
                 Width="1050" Height="750"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 FontFamily="Segoe UI, Malgun Gothic"
                 SnapsToDevicePixels="True">

    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="GridViewColumnHeader">
                <Setter Property="Height" Value="35"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GridViewColumnHeader">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ListViewItem">
                <Setter Property="Height" Value="32"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <GridViewRowPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="17사 TXT 좌표 입력기" Padding="15,5"/>

        <Grid Grid.Row="1" Margin="20,10,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.3*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ui:Card Grid.Row="0" Margin="0" Padding="15,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <iconPacks:PackIconFontAwesome Grid.Column="0" Kind="FileLinesRegular" Width="24" Height="24" Foreground="#2B579A" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="TXT/CSV Data View" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="텍스트 파일을 불러와 좌표를 확인하세요." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:Button Grid.Column="2" x:Name="BtnLoadTxt" Appearance="Info" Click="BtnLoadTxt_Click" Icon="{ui:SymbolIcon Symbol=DocumentSearch24}">
                            <TextBlock Text="파일 불러오기" FontWeight="SemiBold"/>
                        </ui:Button>
                    </Grid>
                </ui:Card>

                <ui:Card Grid.Row="1" Padding="0" Margin="0,5,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Top">
                    <ListView x:Name="DataListView" BorderThickness="0" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Id}" Margin="5,0,0,0" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="X (Easting)" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding X}" Margin="5,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Y (Northing)" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Y}" Margin="5,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Z (Level)" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Z}" Margin="5,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </ui:Card>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ui:Card Grid.Row="0" Padding="15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="Layer24" Margin="0,0,8,0" Filled="True" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="작업 대상 도면" FontWeight="Bold" FontSize="13"/>
                        </StackPanel>
                        <ComboBox x:Name="CboDrawingSelect" HorizontalAlignment="Stretch" DisplayMemberPath="DisplayName"/>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Row="2" Padding="20,25" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,22">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="원 색상" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ComboBox x:Name="CboCircleColor" HorizontalAlignment="Stretch" SelectedIndex="0">
                                    <ComboBoxItem Content="Red (1)"/>
                                    <ComboBoxItem Content="Yellow (2)"/>
                                    <ComboBoxItem Content="Green (3)"/>
                                    <ComboBoxItem Content="Cyan (4)"/>
                                    <ComboBoxItem Content="Blue (5)"/>
                                    <ComboBoxItem Content="Magenta (6)"/>
                                    <ComboBoxItem Content="White (7)"/>
                                    <ComboBoxItem Content="True White (255)"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="원 채우기 색상" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ComboBox x:Name="CboFillColor" HorizontalAlignment="Stretch" SelectedIndex="7">
                                    <ComboBoxItem Content="Red (1)"/>
                                    <ComboBoxItem Content="Yellow (2)"/>
                                    <ComboBoxItem Content="Green (3)"/>
                                    <ComboBoxItem Content="Cyan (4)"/>
                                    <ComboBoxItem Content="Blue (5)"/>
                                    <ComboBoxItem Content="Magenta (6)"/>
                                    <ComboBoxItem Content="White (7)"/>
                                    <ComboBoxItem Content="True White (255)"/>
                                    <ComboBoxItem Content="None"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1" Margin="0,0,0,22">
                            <TextBlock Text="텍스트 색상" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ComboBox x:Name="CboTextColor" HorizontalAlignment="Stretch" SelectedIndex="6">
                                <ComboBoxItem Content="Red (1)"/>
                                <ComboBoxItem Content="Yellow (2)"/>
                                <ComboBoxItem Content="Green (3)"/>
                                <ComboBoxItem Content="Cyan (4)"/>
                                <ComboBoxItem Content="Blue (5)"/>
                                <ComboBoxItem Content="Magenta (6)"/>
                                <ComboBoxItem Content="White (7)"/>
                                <ComboBoxItem Content="True White (255)"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,22">
                            <TextBlock Text="도면층(Layer) 이름" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <ui:TextBox x:Name="TxtLayerName" PlaceholderText="입력할 레이어 이름" Icon="{ui:SymbolIcon Symbol=Tag24}"/>
                        </StackPanel>

                        <Grid Grid.Row="3" Margin="0,0,0,35">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="원 크기" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBox x:Name="TxtCircleSize" Text="50.0" TextAlignment="Right" Icon="{ui:SymbolIcon Symbol=Target24}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="텍스트 크기" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBox x:Name="TxtTextSize" Text="100.0" TextAlignment="Right" Icon="{ui:SymbolIcon Symbol=TextFontSize24}"/>
                            </StackPanel>
                        </Grid>

                        <ui:Button Grid.Row="5" x:Name="BtnSubmit" Appearance="Info" Height="55" HorizontalAlignment="Stretch" Click="BtnSubmit_Click" VerticalAlignment="Bottom">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconFontAwesome Kind="FileImportSolid" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="데이터 도면 입력 시작" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:Button>
                    </Grid>
                </ui:Card>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>